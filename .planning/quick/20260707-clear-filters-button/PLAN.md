# Add Clear Filters Button

## Objectives
1. Add a "مسح الفلاتر" (Clear Filters) button next to the "تطبيق الفلاتر" button.
2. Bind the new button to a function in `main.js` that resets all filter inputs and calls `filterAndRenderDossiers()`.
