---
phase: 01-core-parsing-logic-engine
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - engine.py
  - app.py
autonomous: true
requirements:
  - FILE-01
  - RENAME-01
  - RENAME-02
  - SETT-01

must_haves:
  truths:
    - "System can scan a directory and parse all xlsx files inside"
    - "System renames files to Year_Category.xlsx automatically"
    - "System handles MS Excel PermissionError locks without crashing"
    - "Flask server has active routes /api/scan and /api/settings"
  artifacts:
    - path: "engine.py"
      provides: "Directory scanner and renaming logic"
    - path: "app.py"
      provides: "Flask server and APIs"
---

<objective>
بناء نظام مسح المجلدات وإعادة التسمية التلقائية وتطوير خادم Flask المحلي.

Purpose: ربط محرك التحليل بنظام الملفات الفعلي على القرص الصلب وتوفير واجهة برمجة التطبيقات (APIs) اللازمة للواجهة الأمامية.
Output: تحديث engine.py بإضافة دوال المسح وتغيير الأسماء، وإنشاء ملف app.py لخادم Flask.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-parsing-logic-engine/01-CONTEXT.md
@.planning/phases/01-core-parsing-logic-engine/01-RESEARCH.md
@.planning/phases/01-core-parsing-logic-engine/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: تطوير نظام مسح المجلدات وتعديل أسماء الملفات</name>
  <files>engine.py</files>
  <read_first>engine.py, .planning/phases/01-core-parsing-logic-engine/01-CONTEXT.md</read_first>
  <action>
    إضافة الدوال التالية إلى ملف engine.py:
    1. `scan_directory(dir_path)`: تفحص جميع ملفات `.xlsx` في المجلد، وتستدعي المعالج لقراءة أول سطر يحتوي على بيانات لاستخراج السنة وفئة الملف.
    2. `rename_file_safely(file_path, year, category)`: تعيد تسمية الملف إلى الصيغة `السنة_الرمز.xlsx`. يجب فحص:
       - إذا كان هناك ملف مستهدف بنفس الاسم مسبقاً، تخطي إعادة التسمية وسجل تحذير (منعاً للكتابة فوق البيانات D-01).
       - تغليف العملية بالكامل بكتلة try-except للتعامل مع `PermissionError` (عندما يكون الملف مفتوحاً في Excel).
  </action>
  <verify>python3 -c "import engine; print(hasattr(engine, 'scan_directory'))"</verify>
  <acceptance_criteria>
    - دالة `scan_directory` موجودة وتعمل.
    - دالة `rename_file_safely` تتعامل مع الملفات المكررة والملفات المقفلة بشكل آمن دون توقف البرنامج.
  </acceptance_criteria>
  <done>تم تحديث المحرك وتطوير دوال مسح المجلدات وتعديل الأسماء بنجاح.</done>
</task>

<task type="auto">
  <name>Task 2: إنشاء خادم Flask المحلي</name>
  <files>app.py</files>
  <read_first>engine.py</read_first>
  <action>
    إنشاء ملف app.py لإعداد خادم Flask بالمسارات التالية:
    1. `GET /`: يعرض صفحة الويب الرئيسية (index.html).
    2. `POST /api/scan`: يستقبل مسار المجلد `directory` ويقوم بمسح الملفات وحساب التواريخ وإعادة التسمية، ثم يرجع قائمة الملفات والتحذيرات مرتبة حسب الأجل تصاعدياً.
    3. `GET /api/settings`: يرجع قاموس الإعدادات الحالي منsettings.json.
    4. `POST /api/settings`: يستقبل قيم الإعدادات المعدلة ويحفظها في settings.json.
  </action>
  <verify>python3 -c "import app; print(app.app)"</verify>
  <acceptance_criteria>
    - ملف app.py موجود ويعرّف تطبيق Flask.
    - المسارات الرئيسية `/api/scan` و `/api/settings` موجودة وتستدعي الدوال المناسبة من engine.py.
  </acceptance_criteria>
  <done>تم إنشاء خادم Flask وتهيئة مسارات الواجهة الخلفية بنجاح.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] تشغيل خادم Flask والتأكد من استجابته للطلبات.
</verification>

<success_criteria>
- خادم Flask يعمل محلياً.
- مسح المجلدات وإعادة التسمية التلقائية تتم بشكل سليم.
- حماية النظام من مشاكل قفل الملفات MS Excel Lock.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-parsing-logic-engine/01-02-SUMMARY.md`
</output>
