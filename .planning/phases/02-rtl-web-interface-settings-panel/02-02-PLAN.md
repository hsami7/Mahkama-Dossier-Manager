---
phase: 02-rtl-web-interface-settings-panel
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - static/js/main.js
autonomous: true
requirements:
  - SETT-01
  - SETT-02

must_haves:
  truths:
    - "Clicking scan fetches /api/scan, updates table with data, and shows warnings"
    - "Dossiers table is colored according to urgency (red, orange, green)"
    - "Settings modal displays current category limits and allows user to update them"
    - "LocalStorage stores scanned directory paths and displays them in dropdown"
  artifacts:
    - path: "static/js/main.js"
      provides: "Frontend interactivity and API bindings"
---

<objective>
بناء وتطوير منطق التفاعل البرمجي (JavaScript) وربط واجهة المستخدم بالباك اند وحفظ مسارات المسح.

Purpose: جعل لوحة التحكم حية وتفاعلية بالكامل، وقادرة على الاتصال بـ Flask APIs لحفظ وقراءة البيانات.
Output: ملف static/js/main.js.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rtl-web-interface-settings-panel/02-CONTEXT.md
@.planning/phases/02-rtl-web-interface-settings-panel/02-RESEARCH.md
@.planning/phases/02-rtl-web-interface-settings-panel/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: برمجة منطق التفاعل بالـ DOM واستدعاء API</name>
  <files>static/js/main.js</files>
  <read_first>static/js/main.js, .planning/phases/02-rtl-web-interface-settings-panel/02-RESEARCH.md</read_first>
  <action>
    إنشاء ملف static/js/main.js يحتوي على:
    1. إدارة المسارات السابقة في LocalStorage وعرضها في قائمة منسدلة لتسهيل الفرز السريع.
    2. استدعاء مسار `POST /api/scan` عند الضغط على زر المسح، وتبديل الجدول لعرض حالة التحميل (Skeleton)، ثم استبدالها بصفوف البيانات الفعلية الملونة حسب درجة الاستعجال، وعرض التحذيرات إن وجدت.
    3. التحكم في فتح وإغلاق نافذة الإعدادات المنبثقة (Modal).
    4. استدعاء `GET /api/settings` لتعبئة حقول الإعدادات، واستدعاء `POST /api/settings` عند حفظ التغييرات لتحديث مدد الآجال وإعادة مسح المجلد الحالي فوراً لتحديث الجدول.
  </action>
  <verify>test -f static/js/main.js</verify>
  <acceptance_criteria>
    - وجود ملف static/js/main.js
    - يحتوي على وظائف التفاعل وسحب البيانات وحفظ التفضيلات في LocalStorage.
  </acceptance_criteria>
  <done>تم برمجة وتطوير منطق التفاعل البرمجي بنجاح.</done>
</task>

<task type="auto">
  <name>Task 2: اختبار الواجهة والربط الكامل مع Flask</name>
  <files>app.py</files>
  <read_first>app.py</read_first>
  <action>
    التأكد من أن خادم Flask في app.py يقوم بخدمة ملفات الواجهة بنجاح:
    - مسار `GET /` يقوم بإرجاع قالب index.html بدلاً من النص المؤقت.
    - المجلدات الثابتة static مدمجة ويتم استدعاء ملفات style.css و main.js منها بشكل صحيح.
  </action>
  <verify>python3 -c "import app; print(app.app.template_folder)"</verify>
  <acceptance_criteria>
    - قوالب index.html تخدم بنجاح.
    - خادم Flask يشتغل محلياً دون أخطاء.
  </acceptance_criteria>
  <done>تم التأكد من تكامل الواجهة الأمامية والخلفية وجاهزيتها للعمل.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] تشغيل الخادم والتحقق من عدم وجود أخطاء في الكونسول (Console Errors) بالمتصفح.
</verification>

<success_criteria>
- جميع الملفات التفاعلية مكتملة ومربوطة.
- حفظ المسارات مؤخراً يعمل محلياً.
- تلوين الجدول وحفظ التغييرات في الإعدادات يعمل بالكامل.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rtl-web-interface-settings-panel/02-02-SUMMARY.md`
</output>
